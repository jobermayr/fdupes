This is the latest development version of fdupes.

The dist directory contains the latest fdupes sources. The code may
be incomplete, and possibly non-functional.

To prepare a package for distribution to end users simply change
the version number in dist/Makefile.inc/VERSION and then type
"make tarball" in this directory.


